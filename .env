# Desenvolvimento local. Este arquivo é versionado, então NÃO coloque segredos
# aqui: POSTGRES_PASSWORD e LOVABLE_API_KEY ficam no painel do EasyPanel em
# produção e, localmente, em `.env.local` (ignorado pelo git) ou exportados no
# shell. Referência completa das variáveis: `.env.example`.

POSTGRES_HOST="186.226.112.41"
POSTGRES_PORT="5437"
POSTGRES_DB="app_financeiro_pg"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD=""
POSTGRES_SSL="false"
POSTGRES_SCHEMA="public"

CREATE_USERS_HOME="true"
